@page
@model WebApp.Pages.Answer.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Answers[0].Answer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Answers[0].EChoices)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Answers) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Answer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EChoices)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AnswersId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AnswersId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AnswersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
