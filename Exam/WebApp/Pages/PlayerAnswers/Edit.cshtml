@page
@model WebApp.Pages.PlayerAnswers.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PlayerAnswer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PlayerAnswer!.PlayerAnswerId" />
            <div class="form-group">
                <label asp-for="PlayerAnswer!.CorrectAnswersCount" class="control-label"></label>
                <input asp-for="PlayerAnswer!.CorrectAnswersCount" class="form-control" />
                <span asp-validation-for="PlayerAnswer!.CorrectAnswersCount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PlayerAnswer!.Count" class="control-label"></label>
                <input asp-for="PlayerAnswer!.Count" class="form-control" />
                <span asp-validation-for="PlayerAnswer!.Count" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PlayerAnswer!.PlayerId" class="control-label"></label>
                <select asp-for="PlayerAnswer!.PlayerId" class="form-control" asp-items="ViewBag.PlayerId"></select>
                <span asp-validation-for="PlayerAnswer!.PlayerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
