@page
@using WebApp.Pages.Shared
@using Domain
@model WebApp.Pages.Statistics.Index

<div class="background">

    @if (@Model.Count != 0)
    {
        <div class="alert alert-success text-center" role="alert">Correct asnwers @Model.Count / @Model.CorrectAnswers</div>

    }

     <div class="text-center" id="text-main">Users who have done this quis: @Model.AllCount</div>

    <div class="text-center" id="text-main">Average result: @Model.CalculatedResult%</div>


</div>
